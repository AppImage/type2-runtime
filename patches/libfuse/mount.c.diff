diff --git a/lib/mount.c b/lib/mount.c
index d71e6fc..454e8a0 100644
--- a/lib/mount.c
+++ b/lib/mount.c
@@ -41,7 +41,8 @@
 #define umount2(mnt, flags) unmount(mnt, (flags == 2) ? MNT_FORCE : 0)
 #endif
 
-#define FUSERMOUNT_PROG		"fusermount3"
+#define FUSERMOUNT_PROG		"fusermount"
+#define FUSERMOUNT_PROG3	"fusermount3"
 #define FUSE_COMMFD_ENV		"_FUSE_COMMFD"
 
 #ifndef HAVE_FORK
@@ -119,15 +120,27 @@ static const struct fuse_opt fuse_mount_opts[] = {
 
 static void exec_fusermount(const char *argv[])
 {
-	execv(FUSERMOUNT_DIR "/" FUSERMOUNT_PROG, (char **) argv);
-	execvp(FUSERMOUNT_PROG, (char **) argv);
+	int errnos[4];
+
+	errnos[0] = (execv(FUSERMOUNT_DIR "/" FUSERMOUNT_PROG3, (char **) argv), errno);
+	errnos[1] = (execvp(FUSERMOUNT_PROG3, (char **) argv), errno);
+
+	argv[0] = FUSERMOUNT_PROG;
+	errnos[2] = (execv(FUSERMOUNT_DIR "/" FUSERMOUNT_PROG, (char **) argv), errno);
+	errnos[3] = (execvp(FUSERMOUNT_PROG, (char **) argv), errno);
+
+	fprintf(stderr, "fuse: failed to exec fusermount, tried the following:\n");
+	fprintf(stderr, "\t"FUSERMOUNT_DIR "/" FUSERMOUNT_PROG3" : %s\n", strerror(errnos[0]));
+	fprintf(stderr, "\t"FUSERMOUNT_PROG3" : %s\n", strerror(errnos[1]));
+	fprintf(stderr, "\t"FUSERMOUNT_DIR "/" FUSERMOUNT_PROG" : %s\n", strerror(errnos[2]));
+	fprintf(stderr, "\t"FUSERMOUNT_PROG" : %s\n", strerror(errnos[3]));
 }
 
 void fuse_mount_version(void)
 {
 	int pid = fork();
 	if (!pid) {
-		const char *argv[] = { FUSERMOUNT_PROG, "--version", NULL };
+		const char *argv[] = { FUSERMOUNT_PROG3, "--version", NULL };
 		exec_fusermount(argv);
 		_exit(1);
 	} else if (pid != -1)
@@ -300,7 +313,7 @@ void fuse_kern_unmount(const char *mountpoint, int fd)
 		return;
 
 	if(pid == 0) {
-		const char *argv[] = { FUSERMOUNT_PROG, "-u", "-q", "-z",
+		const char *argv[] = { FUSERMOUNT_PROG3, "-u", "-q", "-z",
 				       "--", mountpoint, NULL };
 
 		exec_fusermount(argv);
@@ -346,7 +359,7 @@ static int setup_auto_unmount(const char *mountpoint, int quiet)
 			}
 		}
 
-		argv[a++] = FUSERMOUNT_PROG;
+		argv[a++] = FUSERMOUNT_PROG3;
 		argv[a++] = "--auto-unmount";
 		argv[a++] = "--";
 		argv[a++] = mountpoint;
@@ -357,7 +370,6 @@ static int setup_auto_unmount(const char *mountpoint, int quiet)
 		snprintf(env, sizeof(env), "%i", fds[0]);
 		setenv(FUSE_COMMFD_ENV, env, 1);
 		exec_fusermount(argv);
-		perror("fuse: failed to exec fusermount3");
 		_exit(1);
 	}
 
@@ -407,7 +419,7 @@ static int fuse_mount_fusermount(const char *mountpoint, struct mount_opts *mo,
 			}
 		}
 
-		argv[a++] = FUSERMOUNT_PROG;
+		argv[a++] = FUSERMOUNT_PROG3;
 		if (opts) {
 			argv[a++] = "-o";
 			argv[a++] = opts;
@@ -421,7 +433,6 @@ static int fuse_mount_fusermount(const char *mountpoint, struct mount_opts *mo,
 		snprintf(env, sizeof(env), "%i", fds[0]);
 		setenv(FUSE_COMMFD_ENV, env, 1);
 		exec_fusermount(argv);
-		perror("fuse: failed to exec fusermount3");
 		_exit(1);
 	}
 
